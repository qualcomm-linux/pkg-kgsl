#!/bin/bash
set -e

case "$1" in
    remove|purge)
        # Rebuild initramfs without msm_kgsl module
        if command -v update-initramfs > /dev/null 2>&1; then
            update-initramfs -u -k all || true
        fi
        ;;
esac

#DEBHELPER#
